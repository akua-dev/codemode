name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    runs-on: akua-heavy-ci-v2
    steps:
      - uses: actions/checkout@v7

      - uses: jdx/mise-action@v4

      - run: task install

      - run: task ci
